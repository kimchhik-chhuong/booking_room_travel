import 'package:flutter/material.dart';

class RatanakiriPage extends StatelessWidget {
  const RatanakiriPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ratanakiri')),
      body: const Center(child: Text('Welcome to Ratanakiri')),
    );
  }
}